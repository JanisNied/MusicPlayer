## MusicPlayer [with Laravel]
Quick instructions

Install dependencies: Composer, Laravel, PHP, MySQL Server

1. Run npm install && npm run build within the application's directory
2. composer install
3. composer dev run
